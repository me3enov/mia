//import default class
import Layer from './Layer.js';

export class LayerIndex extends Layer {
  //constructor for the "LayerIndex" class
  constructor(settings) {
    //default settings
    super(settings);
  }
}